public class BuiltinFunctionalInterface {
    public static void main(String[] args) {
        //Predicate, function, supplier and consumer

        //Predicate is  functional interface that takes any type as input and returns bool as output
        // filter in stream API accepts predicate as argument.
    }
}
